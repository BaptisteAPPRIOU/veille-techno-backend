# E2e test environment (`npm run test:e2e`): dedicated database reset before each test.
# Tracked intentionally: this file contains no sensitive values.
DATABASE_URL="postgresql://kanban:kanban@localhost:5433/kanban_test?schema=public"
JWT_SECRET="test-secret-only-do-not-use-in-production"
JWT_EXPIRES_IN="1h"
PORT=3001
